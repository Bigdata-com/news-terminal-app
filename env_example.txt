BIGDATA_API_KEY=YOUR_KEY_HERE

# Gemini AI — pick Vertex (GCP) OR AI Studio (API key), not both.

# Method 1: Vertex AI with user ADC (local: gcloud auth application-default login)
GOOGLE_GENAI_USE_VERTEXAI=True
GOOGLE_CLOUD_PROJECT=YOUR_PROJECT_ID
GOOGLE_CLOUD_LOCATION=us-central1
# Optional: USE_ADC=true is ignored when Vertex is enabled; credentials come from ADC or SA file.

# Method 2: Vertex AI with service account JSON (path must exist)
# GOOGLE_GENAI_USE_VERTEXAI=True
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GOOGLE_CLOUD_PROJECT=YOUR_PROJECT_ID
# GOOGLE_CLOUD_LOCATION=us-central1

# Method 3: AI Studio (Gemini API key) — turn OFF Vertex for this path
# GOOGLE_GENAI_USE_VERTEXAI=False
# GEMINI_API_KEY=YOUR_KEY_HERE
